{
  "name": "forge-cursor-plugin",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=18.0.0" },
  "description": "Cursor plugin adding Forge—structured development workflow with skills, agents, and commands",
  "scripts": {
    "test": "npm run test:unit && npm run test:skills",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "test:skills": "bats test/skills"
  },
  "dependencies": {
    "node-html-markdown": "^2.0.0"
  },
  "devDependencies": {
    "bats": "^1.11.0",
    "vitest": "^2.0.0"
  }
}
